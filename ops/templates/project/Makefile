# 프로젝트 dev/Makefile — thin wrapper
# 모든 실제 타겟은 local-claude 의 Makefile.common 에서 include 됨.
# Makefile.common 변경 시 이 프로젝트도 즉시 반영 (재복사 불필요).

LOCAL_CLAUDE ?= $(HOME)/workspace/local-claude
PROJECT       = __PROJECT__
STACK         = __STACK__

include $(LOCAL_CLAUDE)/ops/common/Makefile.common
